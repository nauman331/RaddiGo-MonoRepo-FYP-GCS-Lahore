{
  "name": "raddigo-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "bun --filter './apps/*-service' dev & bun --filter gateway dev"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "jsonwebtoken": "^9.0.0",
    "nodemailer": "^7.0.12"
  },
  "devDependencies": {
    "bun-types": "^1.3.5"
  }
}